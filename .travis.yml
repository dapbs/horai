language: python
sudo: false

python:
  - "3.6"
  - "3.7-dev"  # 3.7 development branch

install:
  - pip intsall pipenv
  - pipenv install

os:
  - os

branches:
  only:
    - master

script:
  - python -c 'from horai import SeasonalForecast;'
